# gen — 生图工作空间

`/generate-image` 技能的输出目录，存放生成的图片和 metadata。

## 子目录

| 目录 | 用途 |
|------|------|
| `outputs/` | 生成的图片和 metadata.json（按时间戳+名称分子目录） |

## 对应技能

`/generate-image` — 单张生成（API）、Codex CLI 生图（无需 API Key）。
