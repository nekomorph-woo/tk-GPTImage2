# collected — 外部收集

`/collect-image-2-prompt` 技能的私有目录，存放从 X 等外部平台收集的结构化 prompt。

## 文件命名

`<编号>_<平台>_<用户名>_<关键词>.md`，编号自动递增（如 `001_@user_x_cinematic.md`）。

## 文件格式

每条记录包含：用途、标签、Prompt 正文、图片样例、来源链接等字段，按模板结构化存储。

## 对应技能

`/collect-image-2-prompt` — 在 X 上搜索 GPT Image 2 提示词并收集整理。
